# Set AZURE_MODE=true to use live Azure data (requires Azure CLI login or service principal)
AZURE_MODE=false

# Required only if AZURE_MODE=true
AZURE_SUBSCRIPTION_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
